# Trading Terminal Pro — Environment Configuration

# Server
PORT=3001

# No API keys required by default — Yahoo Finance data is fetched via yfinance (Python)
# For production Yahoo Finance API access, you may need:
# YAHOO_API_KEY=your_key_here
